
using System.IO;
using System.Runtime.Serialization.Formatters.Binary;
using System;
using System.Collections.Generic;


namespace IOSerialize
{
    class Program
    {
        static void Main(string[] args)
        {
            string filepath = @"D:\Study\Temp1";
            string filepath2 = @"D:\Study\Temp2";

            // if (!Directory.Exists(filepath))
            // {
            //     Directory.CreateDirectory(filepath);

            // }
            // else
            // {
            //     //  Directory.Move(filepath, filepath2);

            // }

            // var EnumerateDirectories = Directory.EnumerateDirectories(filepath2);
            // var EnumerateFiles = Directory.EnumerateFiles(filepath2);
            // var GetCreationTime = Directory.GetCreationTime(filepath2);
            // var GetCreationTimeUtc = Directory.GetCreationTimeUtc(filepath2);
            // var GetDirectories = Directory.GetDirectories(filepath2);
            // var GetFiles = Directory.GetFiles(filepath2);
            // var GetLastWriteTime = Directory.GetLastAccessTime(filepath2);

            // var txtPath = Path.Combine(filepath2, "zidingyitxt.txt");
            // //  File.Create(txtPath);
            // var content = new List<string>() { "123", "456", "6789" };

            // File.AppendAllLines(txtPath, content);
            // var ReadAllLines = File.ReadLines(txtPath);

            // var dirList = Common.GetAllDir(filepath2);




            Console.ReadLine();
        }
    }




}
